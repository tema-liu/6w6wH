import styled from "styled-components";
import Header from "../../component/header";

function SearchResult() {
  const num = 5;

  return <Header title={`SearchResult(${num})`} />;
}

export default SearchResult;
